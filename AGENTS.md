# AGENTS

- Place temporary mockups, screenshots, and exploratory HTML artifacts in the repo-local `scratch/` directory, not in tracked source directories.
- Keep `scratch/` ignored unless the user explicitly asks to track a specific artifact.
